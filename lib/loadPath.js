import {resolve} from 'path';
import dirs from './dirs';

export default loadPath;

/**
 * Expand a path for load-based modules.
 * @param {String} path - An unresolved path.
 */
function loadPath(path) {
  
}
